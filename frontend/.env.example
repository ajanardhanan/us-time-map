# API Configuration (Optional - if you want to use Lambda backend instead of client-side calculation)
# REACT_APP_API_ENDPOINT=https://YOUR-API-ID.execute-api.us-east-1.amazonaws.com/prod/times

# Feature Flags
# REACT_APP_USE_API=false

# CloudFront Distribution (for reference)
# CLOUDFRONT_DISTRIBUTION_ID=YOUR-DISTRIBUTION-ID
